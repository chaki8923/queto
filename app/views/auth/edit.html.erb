

<div class="p-5 mt-5">
  <h1>ユーザー編集</h1>
    <%= form_for(@user,model: @user,url: edit_auth_path,html: {class: 'form-group'}) do |f| %>
      <div class="mb-3">
        <%= f.label :name,class: 'form-label' %>
        <%= f.text_field :name,class: 'form-control' %>
        <% @user.errors.full_messages_for(:name).each do |message| %>
          <span class="alert"><%= message %></span>
        <% end %> 
      </div>

      
      <%= f.submit "Edit", class: 'btn btn-primary' %>
      <% end %>
       <a class="btn btn-info" href="/auth/<%=@current_user.id%>/avatar">アイコン登録</a>
  </div>